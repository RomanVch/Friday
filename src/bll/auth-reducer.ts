
const initialState={
    auth:false
}

type initialStateType = {
    auth: boolean;
}


export const authReducer = (state: initialStateType = initialState, action: authACType): initialStateUsersType => {

    switch (action.type) {

        default:
            return state

    }
}

